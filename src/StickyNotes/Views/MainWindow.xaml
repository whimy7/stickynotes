<Window x:Class="StickyNotes.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="桌面便签"
        Width="940"
        Height="620"
        MinWidth="760"
        MinHeight="480"
        WindowStartupLocation="CenterScreen"
        Background="#F5F6F8"
        FontFamily="Microsoft YaHei UI"
        FontSize="13">
    <Window.Resources>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="#25272B" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="15,9" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#303238" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="#D9DCE1" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Margin" Value="0,0,7,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#F0F1F3" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="76" />
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" BorderBrush="#E2E4E8" BorderThickness="0,0,0,1">
            <Grid Margin="24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="34" Height="34" Background="#FFF2A8" BorderBrush="#E6D57C" BorderThickness="1" CornerRadius="4" Margin="0,0,12,0">
                        <TextBlock Text="&#xE70B;" FontFamily="Segoe Fluent Icons" FontSize="17" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#46432E" />
                    </Border>
                    <TextBlock Text="桌面便签" FontSize="22" FontWeight="SemiBold" Foreground="#202226" VerticalAlignment="Center" />
                </StackPanel>
                <Button Grid.Column="1" Style="{StaticResource PrimaryButtonStyle}" Click="CreateNote_Click" VerticalAlignment="Center" AutomationProperties.Name="新建便签">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE710;" FontFamily="Segoe Fluent Icons" FontSize="14" Margin="0,0,7,0" />
                        <TextBlock Text="新建便签" />
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="24,20,24,12">
            <DataGrid x:Name="NotesGrid"
                      ItemsSource="{Binding Notes}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserReorderColumns="False"
                      CanUserResizeRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#ECEEF1"
                      BorderBrush="#DEE1E5"
                      BorderThickness="1"
                      Background="White"
                      RowBackground="White"
                      AlternatingRowBackground="#FAFAFB"
                      RowHeight="64"
                      ColumnHeaderHeight="38"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      MouseDoubleClick="NotesGrid_MouseDoubleClick">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F7F8F9" />
                        <Setter Property="Foreground" Value="#686C73" />
                        <Setter Property="FontWeight" Value="Normal" />
                        <Setter Property="BorderBrush" Value="#DEE1E5" />
                        <Setter Property="BorderThickness" Value="0,0,0,1" />
                        <Setter Property="Padding" Value="12,0" />
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Padding" Value="12,0" />
                        <Setter Property="VerticalContentAlignment" Value="Center" />
                    </Style>
                    <Style TargetType="DataGridRow">
                        <Setter Property="BorderThickness" Value="0" />
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="名称" Width="210">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid>
                                    <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                                             BorderThickness="0"
                                             Background="Transparent"
                                             Foreground="#24262A"
                                             FontWeight="SemiBold"
                                             Padding="0,7"
                                             LostKeyboardFocus="NoteTitle_LostKeyboardFocus"
                                             ToolTip="编辑名称" />
                                    <TextBlock Text="未命名便签"
                                               Foreground="#969AA1"
                                               FontWeight="Normal"
                                               VerticalAlignment="Center"
                                               IsHitTestVisible="False"
                                               Visibility="{Binding Title, Converter={StaticResource StringEmptyToVisibilityConverter}}" />
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="内容预览" Width="*" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Preview}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Preview}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="修改时间" Binding="{Binding DisplayUpdatedAt}" Width="105" IsReadOnly="True" />
                    <DataGridTextColumn Header="状态" Binding="{Binding IsVisible, Converter={StaticResource BooleanToStatusConverter}}" Width="78" IsReadOnly="True" />
                    <DataGridTemplateColumn Header="颜色" Width="64">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Width="26" Height="26" Background="{Binding BackgroundColor, Converter={StaticResource ColorToBrushConverter}}" BorderBrush="#BFC2C7" BorderThickness="1" Tag="{Binding}" Click="Color_Click" Cursor="Hand" ToolTip="更换颜色" AutomationProperties.Name="更换颜色" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="操作" Width="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Style="{StaticResource ActionButtonStyle}" Tag="{Binding}" Click="ToggleVisibility_Click" Content="{Binding IsVisible, Converter={StaticResource BooleanToDisplayActionConverter}}" AutomationProperties.Name="显示或隐藏便签" />
                                    <Button Style="{StaticResource ActionButtonStyle}" Tag="{Binding}" Click="DeleteNote_Click" Width="34" Padding="0" ToolTip="删除便签" AutomationProperties.Name="删除便签">
                                        <TextBlock Text="&#xE74D;" FontFamily="Segoe Fluent Icons" FontSize="14" />
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding Notes.Count, Mode=OneWay, Converter={StaticResource ZeroToVisibilityConverter}}" IsHitTestVisible="False">
                <TextBlock Text="&#xE70B;" FontFamily="Segoe Fluent Icons" FontSize="38" Foreground="#B6BAC1" HorizontalAlignment="Center" />
                <TextBlock Text="暂无便签" FontSize="16" Foreground="#71757C" Margin="0,12,0,0" HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" Background="#F5F6F8" BorderBrush="#E2E4E8" BorderThickness="0,1,0,0">
            <TextBlock VerticalAlignment="Center" Margin="24,0" Foreground="#747880">
                <Run Text="共 " /><Run Text="{Binding Notes.Count, Mode=OneWay}" FontWeight="SemiBold" /><Run Text=" 张便签" />
            </TextBlock>
        </Border>
    </Grid>
</Window>
